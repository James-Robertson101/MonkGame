#include <iostream>
#include "Dungeon.h"
int main() {
    Dungeon dungeon;
    dungeon.generate();   // Generate the random dungeon
    dungeon.explore();    // Begin exploring

    return 0;

    return 0;
}